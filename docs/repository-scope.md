# Open Repository Scope

The Open repository is an Apache-2.0 public surface, not a source mirror of
ORCHADYN Core. It may distribute SDK clients, examples, API specifications,
documentation, and release metadata. It must not contain the Core planner,
verifier, OPIR implementation, or EE source.

See the Core repository's `docs/repository-boundaries.md` for the full release
contract.
